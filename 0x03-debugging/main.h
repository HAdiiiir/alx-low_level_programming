#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

void positive_or_negative(int i);
int Largest_number(int a, int b);
void print_remaining_days(int month, int year, int day);
int convert_day(int minth, int day);

#endif
